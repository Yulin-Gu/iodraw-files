```mermaid
graph LR
    subgraph 分割产生式
        A[inputString] --> B{i < inputString.length()}
        B -- 否 --> E[返回 v]
        B -- 是 --> C{inputString[i] == '\n'}
        C -- 是 --> D[v.push_back(inputString.substr(from, len))]
        D --> B
        C -- 否 --> B
        E --> F[结束]
    end
```